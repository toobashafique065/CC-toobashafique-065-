---
- name: Install Nginx
  yum:
    name: nginx
    state: present

- name: Deploy Nginx load balancer config
  template:
    src: nginx_frontend.conf.j2
    dest: /etc/nginx/conf.d/default.conf

- name: Restart Nginx
  service:
    name: nginx
    state: restarted
